# Golden Rules — Nextflow

1. **NEVER** lancer Nextflow depuis le répertoire du pipeline — toujours depuis `~/Run` ou `~/Run2`.
2. **NEVER** hardcoder les chemins ou tokens dans les fichiers de configuration.
